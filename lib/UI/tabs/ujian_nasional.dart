import 'package:flutter/material.dart';

class UjianNasionalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Ujian nasional'),
    );
  }
}